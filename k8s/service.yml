apiVersion: v1
kind: Service
metadata:
  name: ting
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ting"
    port: 8080
  selector:
    app: ting
