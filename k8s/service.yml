apiVersion: v1
kind: Service
metadata:
  name: note
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "note"
    port: 8080
  selector:
    app: note
