apiVersion: v1
kind: Service
metadata:
  name: like
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "like"
    port: 8080
  selector:
    app: like
