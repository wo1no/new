apiVersion: v1
kind: Service
metadata:
  name: dicd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dicd"
    port: 8080
  selector:
    app: dicd
