apiVersion: v1
kind: Service
metadata:
  name: oppo
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "oppo"
    port: 8080
  selector:
    app: oppo
