apiVersion: v1
kind: Service
metadata:
  name: popo
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "popo"
    port: 8080
  selector:
    app: popo
