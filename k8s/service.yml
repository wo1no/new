apiVersion: v1
kind: Service
metadata:
  name: toto
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "toto"
    port: 8080
  selector:
    app: toto
