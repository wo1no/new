apiVersion: v1
kind: Service
metadata:
  name: voto
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "voto"
    port: 8080
  selector:
    app: voto
