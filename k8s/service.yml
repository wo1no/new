apiVersion: v1
kind: Service
metadata:
  name: dell
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "dell"
    port: 8080
  selector:
    app: dell
