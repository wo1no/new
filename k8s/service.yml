apiVersion: v1
kind: Service
metadata:
  name: meuz
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "meuz"
    port: 8080
  selector:
    app: meuz
