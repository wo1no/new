apiVersion: v1
kind: Service
metadata:
  name: boss
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "boss"
    port: 8080
  selector:
    app: boss
