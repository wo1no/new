apiVersion: v1
kind: Service
metadata:
  name: asus
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "asus"
    port: 8080
  selector:
    app: asus
